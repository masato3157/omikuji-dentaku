# ⛩️ おみくじ電卓 (Omikuji Dentaku) 仕様書

**Version:** 0.8.1  
**Last Updated:** 2026-02-02

## 1. 概要 (Overview)
「おみくじ電卓」は、通常の電卓機能に日本の伝統的な「おみくじ」の要素を融合させたエンターテインメントWebアプリケーションです。
無骨でインダストリアルな筐体デザインと、結果表示時の純和風な演出のギャップを特徴とします。
計算結果が表示されると同時に運勢が占われ、日々の計算業務にささやかな楽しみを提供します。

## 2. 技術スタック (Technology Stack)
*   **Framework:** React 19.2.0 + Vite 7.2.4
*   **Styling:** Tailwind CSS v4.1.18 (PostCSS)
*   **Icon Library:** Lucide React
*   **Routing:** なし（シングルページアプリケーション）
*   **Language:** JavaScript (ES Modules)

## 3. 基本仕様 (Basic Specifications)

### 3.1 画面レイアウト
*   **基本サイズ:** 電卓本体の幅は `340px` に固定。
*   **オートスケール (Smart Auto-Scale):**
    *   ウィンドウサイズ（幅・高さ）に応じて、電卓本体のスケール（`transform: scale()`）を自動調整。
    *   **計算式:** `Math.min(1, windowWidth / 360, windowHeight / 720)`
    *   **制約:** スケール係数は `0.7`（最小）〜 `1.0`（最大）の範囲に制限。PCなどの大画面では等倍表示、スマホ等の小画面では画面内に収まるように縮小表示されます。

### 3.2 デザインコンセプト
*   **テーマ:** Industrial Premium Design × Pure Japanese Style
*   **筐体:** シルバーヘアライン仕上げの金属質感、角丸（Radius 20px）。
*   **フォント:**
    *   **タイトル・主要テキスト:** `Yomogi` (脱力系手書きフォント)
    *   **数値ディスプレイ:** Monospace系フォント
    *   **おみくじ結果:** `Shippori Mincho` (明朝体・縦書き対応)

## 4. 詳細機能仕様 (Functional Specifications)

### 4.1 電卓機能 (Calculator)
一般的な12桁電卓としての機能を持ちます。

*   **入力制限:** 最大12桁（小数点含む）。
*   **演算機能:** 加算(`+`)、減算(`-`)、乗算(`×`)、除算(`÷`)。
*   **補助機能:**
    *   `AC`: オールクリア（計算状態、表示、変数をリセット）。
    *   `+/-`: 符号反転。
    *   `%`: パーセント計算（1/100にする）。
*   **表示制御:**
    *   計算結果が整数であればそのまま表示。
    *   小数の場合、最大8桁まで有効数字を表示（`parseFloat(result.toFixed(8))`）。
    *   文字数オーバーフロー時は `text-overflow: ellipsis` で省略表示しレイアウト崩れを防止。

### 4.2 おみくじ機能 (Omikuji Feature)
計算完了（`=` ボタン押下）時に、おみくじロジックが発動します。

#### A. 発動フロー
1.  ユーザーが計算を行い、`=` を押下。
2.  計算結果が正常（有限数値）である場合、ファンファーレ音（ピヨーン）と共に詳細画面（モーダル）が開く。
3.  詳細画面下段に、抽選された運勢が表示される。

#### B. 抽選ロジック (Probability Logic)
重み付け抽選（Weighted Random Selection）を採用しています。

| 運勢 | 確率 | 重み (Weight) |
| :--- | :--- | :--- |
| **大吉** | 10% | 10 |
| **中吉** | 20% | 20 |
| **吉** | 40% | 40 |
| **末吉** | 20% | 20 |
| **凶** | 10% | 10 |

#### C. おみくじデータ構成
各運勢には以下のデータが紐付いています（`src/constants/fortunes.js`）。
1.  **メインメッセージ (Message):** 運勢ごとの一言（例：「最高の一日になります」）。各運勢につき3〜5種類からランダム選択。
2.  **詳細項目 (Items):** 生活に関連する10項目（願望、失物、旅立、商売、学問、恋愛、転居、健康、就職、家庭）から、ランダムに **3項目** が選出され表示されます。

#### D. フック文言 (Hook Text)
電卓上部のオレンジ色のボタンには、計算完了時にユーザーの興味を惹く「フック文言」がランダムに表示されます（例：「今日の運勢を見る」「運命の分かれ道」など10種類）。

### 4.3 詳細画面モーダル (Result Modal)
おみくじ結果を表示する純和風デザインのモーダルです。

*   **外観:** 背景色 `#fffbf0`（生成り色）、枠線 `#c53d43`（朱色）の二重線（4px double）。
*   **レイアウト:** 
    1.  **上段:** 計算結果表示エリア（コピー機能付き）。
    2.  **中段:** 広告バナーエリア（320x50）。
    3.  **下段:** おみくじ詳細エリア。
*   **おみくじ表示（疑似縦書き）:**
    *   右側：運勢スタンプ（赤丸囲み）＋メインメッセージ。
    *   左側：選出された3項目の詳細（項目名＋結果）。
    *   実装：CSS `writing-mode` の互換性問題を回避するため、Flexbox (`flex-col`) を用いて文字を1文字ずつスパンで積み上げる「疑似縦書きコンポーネント」を使用。

### 4.4 特殊機能：0除算爆発 (Easter Egg: Division by Zero)
計算結果が `Infinity` または `-Infinity` （ゼロ除算など）になった場合に発動する隠し機能です。

*   **発動条件:** 結果が `Infinity` / `-Infinity` になる計算（例: `1 ÷ 0 =`）。
*   **演出 (Explosion Effect):**
    1.  **爆発:** Web Audio APIによる激しい爆発音。
    2.  **ボタン消失:** 全19個のボタンがランダムな方向へ吹き飛び（`transform: translate/rotate/scale(0)`）、画面から消える。
    3.  **ブラックホール:** ボタンがあった位置には、黒い穴（Black hole）だけが残る。
    4.  **グリッチ:** ディスプレイの数字が乱れ、ランダムな記号列（`!@#$%...`）に変化。
    5.  **メッセージ:** 電卓上部のパネルが開き、「あーあ、壊れちゃった。」というメッセージと「直す」ボタンにより復旧を促す。
*   **復旧:** 「直す」ボタン押下で、全ボタンが元の位置に戻り、計算状態がリセットされる。

### 4.5 サウンド仕様 (Sound Specifications)
Web Audio APIを使用し、外部音声ファイルを使用せずにリアルタイム合成音を生成します。

| イベント | 音の種類 | 特徴 |
| :--- | :--- | :--- |
| **ボタン押下** | Thocky Click | メカニカルキーボードのような、高域をカットしたプラスチック音。 |
| **計算完了 (=)** | Piyoon | 三角波を用いた、間の抜けたスライド音。 |
| **モーダル表示** | Pathetic Sound | 「情けない」雰囲気の下降音（周波数ダウン）。 |
| **爆発** | Explosion | ホワイトノイズとフィルタスイープを組み合わせた激しい破裂音。 |

### 4.6 広告機能 (Ads)
*   Lineアプリ内ブラウザ判定を行い、出し分けを実施。
    *   Line内: `/ad_line.html`
    *   その他: `/ad_web.html`
*   `Range.createContextualFragment` を使用して安全にHTMLフラグメントとして挿入。

## 5. 現在の課題・制限事項
*   **縦書き表示:** Android/iOS間での表示崩れを防ぐため、CSSの `writing-mode` ではなく独自の垂直配置コンポーネントを使用しているため、複雑な禁則処理などは未対応（現状の短い文言では問題なし）。

---
*End of Specification*
